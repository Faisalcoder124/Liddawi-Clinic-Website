export default function handler(req,res){ res.setHeader('Access-Control-Allow-Origin','*'); if(req.method==='OPTIONS') return res.status(204).end(); const keys=Object.keys(process.env); return res.status(200).json({ hasGithub: !!process.env.GITHUB_TOKEN, githubPreview: process.env.GITHUB_TOKEN ? process.env.GITHUB_TOKEN.slice(0,10)+'***' : null, keys: keys.filter(k=>k.includes('GITHUB')||k.includes('TOKEN')||k.includes('SUPABASE')||k.includes('VERCEL')).sort(), allCount: keys.length }); }
